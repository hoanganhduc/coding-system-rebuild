# Global Codex Workflow Rules

This file is intentionally kept as a short pointer to avoid duplicating active runtime instructions.

- The live session instruction source is `~/.codex/AGENTS.md`.
- For quick operator examples, use `~/.codex/instructions/research-quick-actions.md`.
- Keep behavioral rules in `AGENTS.md` and avoid reintroducing parallel copies here.
