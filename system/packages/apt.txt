# apt packages installed by `make prepare` (phase: system). One per line.
# Heavy items are annotated; SKIP_* toggles control them (see bin/prepare.sh).
curl
jq
pandoc
unzip
7zip
pipx
python3-venv
python3-pip
python3-yaml
stress
syncthing
gpg
git
make
docker.io
gh
# xtradeb PPA required for the next three on stock Ubuntu 24.04 (see prepare.sh):
chromium
chromium-driver
calibre
# heavy (~5.5GB) — SKIP_LATEX=1 to skip:
texlive-full
